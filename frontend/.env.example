# Copy this file to `.env` in `frontend/` if you need to override the default.
# `.env` itself is gitignored.

# Base URL of the backend API. Defaults to http://localhost:8000 if unset.
VITE_API_BASE_URL=http://localhost:8000
