// Placeholder (Story 1.5) -- real graph visualization arrives in Epic 4.
// Heading + coming-soon copy only, so Epic 4 can replace this body
// without touching the shell around it.
export default function GraphPage() {
  return (
    <>
      <h1 className="text-xl font-bold text-[var(--text)]">Graph Preview</h1>
      <p className="mt-2 text-sm text-[var(--text2)]">Coming in Epic 4.</p>
    </>
  )
}
