// Placeholder (Story 1.5) -- real account/appearance settings (Profile,
// Change Password, Appearance, Delete Account cards) arrive in Epic 5.
// Heading + coming-soon copy only, so Epic 5 can replace this body
// without touching the shell around it.
export default function SettingsPage() {
  return (
    <>
      <h1 className="text-xl font-bold text-[var(--text)]">User Settings</h1>
      <p className="mt-2 text-sm text-[var(--text2)]">Coming in Epic 5.</p>
    </>
  )
}
