// Placeholder (Story 1.5) -- real grounded Q&A UI arrives in Epic 3.
// Heading + coming-soon copy only, so Epic 3 can replace this body
// without touching the shell around it.
export default function ChatPage() {
  return (
    <>
      <h1 className="text-xl font-bold text-[var(--text)]">Chat</h1>
      <p className="mt-2 text-sm text-[var(--text2)]">Coming in Epic 3.</p>
    </>
  )
}
