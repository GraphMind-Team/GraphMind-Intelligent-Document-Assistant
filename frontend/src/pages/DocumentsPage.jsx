// Placeholder (Story 1.5) -- real document ingestion/library UI arrives in
// Epic 2. Heading + coming-soon copy only, so Epic 2 can replace this
// body without touching the shell around it.
export default function DocumentsPage() {
  return (
    <>
      <h1 className="text-xl font-bold text-text">Documents</h1>
      <p className="mt-2 text-sm text-text2">Coming in Epic 2.</p>
    </>
  )
}
