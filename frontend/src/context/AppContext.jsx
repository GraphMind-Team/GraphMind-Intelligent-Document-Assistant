// Shared React context for the app.
//
// Stub for Story 1.1 (running project skeleton). No global state exists
// yet -- this file exists so the `src/context/` directory shape is
// correct from day one, per the structural seed. Real context providers
// (e.g. auth session state) arrive in later stories.

import { createContext } from 'react'

export const AppContext = createContext(null)
